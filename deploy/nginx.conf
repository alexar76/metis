# Metis node reverse proxy — metis.modelmarket.dev + skopos.modelmarket.dev
#
# Canonical config mounted into `metis-nginx` (nginx:alpine, :80/:443):
#
#   docker run -d --name metis-nginx --restart unless-stopped \
#     --network metisnet -p 80:80 -p 443:443 \
#     -v /var/www/metis-landing:/usr/share/nginx/html:ro \
#     -v /opt/metis/deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro \
#     -v /var/www/certbot:/var/www/certbot:ro \
#     -v /opt/metis/deploy/letsencrypt:/etc/letsencrypt:ro \
#     nginx:alpine
#
# Upstreams on `metisnet`:
#   metis:8080           — Metis API (metis.modelmarket.dev)
#   metis-skopos:8501    — SKOPOS Streamlit UI (skopos.modelmarket.dev)
#   metis-skopos:8502    — SKOPOS public GET /healthz
#
# SKOPOS container must be on metisnet: see metis/deploy/skopos-test/docker-compose.yml
# TLS: certbot webroot — see deploy/README.md

limit_req_zone $binary_remote_addr zone=metis_api:10m rate=120r/m;

# --- HTTP: domain -> redirect to HTTPS (ACME challenge kept reachable) ---
server {
  listen 80;
  server_name metis.modelmarket.dev;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  location / { return 301 https://$host$request_uri; }
}

# --- HTTP: bare IP / anything else -> serve over HTTP ---
server {
  listen 80 default_server;
  server_name _;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri $uri/ /index.html; }
  # Live cognition trace (SSE) — must NOT buffer, or council runs (8-15s) are
  # withheld and delivered as one lump. Exact match wins over the /v1/ prefix.
  location = /v1/verify/stream {
    limit_req zone=metis_api burst=20 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_cache off;
    gzip off;
    chunked_transfer_encoding on;
    proxy_read_timeout 420s;
  }
  # Factory confidence-gate — runs can take up to Metis _RUN_TIMEOUT (300s).
  location = /v1/verify {
    limit_req zone=metis_api burst=40 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 10s; proxy_read_timeout 420s;
  }
  location /v1/ {
    limit_req zone=metis_api burst=40 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 10s; proxy_read_timeout 420s;
  }
  location = /health { proxy_pass http://metis:8080/health; }
  location /aimarket/ {
    limit_req zone=metis_api burst=20 nodelay;
    proxy_pass http://metis:8080; proxy_set_header Host $host; proxy_read_timeout 420s;
  }
}

# --- HTTPS: domain ---
server {
  listen 443 ssl;
  http2 on;
  server_name metis.modelmarket.dev;
  ssl_certificate     /etc/letsencrypt/live/metis.modelmarket.dev/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/metis.modelmarket.dev/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;
  add_header Strict-Transport-Security "max-age=31536000" always;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri $uri/ /index.html; }
  # Live cognition trace (SSE) — must NOT buffer, or council runs (8-15s) are
  # withheld and delivered as one lump. Exact match wins over the /v1/ prefix.
  location = /v1/verify/stream {
    limit_req zone=metis_api burst=20 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_cache off;
    gzip off;
    chunked_transfer_encoding on;
    proxy_read_timeout 420s;
  }
  # Factory confidence-gate — runs can take up to Metis _RUN_TIMEOUT (300s).
  location = /v1/verify {
    limit_req zone=metis_api burst=40 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 10s; proxy_read_timeout 420s;
  }
  location /v1/ {
    limit_req zone=metis_api burst=40 nodelay;
    proxy_pass http://metis:8080;
    proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 10s; proxy_read_timeout 420s;
  }
  location = /health { proxy_pass http://metis:8080/health; }
  location /aimarket/ {
    limit_req zone=metis_api burst=20 nodelay;
    proxy_pass http://metis:8080; proxy_set_header Host $host; proxy_read_timeout 420s;
  }
}

# --- SKOPOS (skopos.modelmarket.dev) — HTTP redirect + HTTPS proxy ---
server {
  listen 80;
  server_name skopos.modelmarket.dev;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl;
  http2 on;
  server_name skopos.modelmarket.dev;
  ssl_certificate     /etc/letsencrypt/live/skopos.modelmarket.dev/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/skopos.modelmarket.dev/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;
  add_header Strict-Transport-Security "max-age=31536000" always;

  location = /healthz {
    proxy_pass http://metis-skopos:8502/healthz;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    # Unauthenticated, and each call opens a PostgreSQL connection and runs a
    # COUNT. Now that the API is threaded, a flood turns 1:1 into concurrent
    # database connections, so it gets the same bucket as the rest of the API.
    limit_req zone=metis_api burst=10 nodelay;
  }

  # Floating AI assistant API (token-authenticated chat + ping) — same-origin so
  # the widget in the /app dashboard reaches it without CORS.
  location /agent/ {
    proxy_pass http://metis-skopos:8502;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Authorization $http_authorization;
    client_max_body_size 256k;
    proxy_connect_timeout 10s;
    proxy_read_timeout 120s;
  }

  # SKOPOS host agents reporting in. Machine-to-machine only: the signature is
  # over the raw body, so nothing here may buffer to disk, rewrite, or
  # transcode the request. X-Real-IP is what the ingest path records as the
  # reporting address.
  location /node/ {
    proxy_pass http://metis-skopos:8502;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    # Reports are capped at 8 MB by the agent and again by the API; nginx
    # rejecting a larger one first keeps it out of the Python process entirely.
    client_max_body_size 8m;
    limit_req zone=metis_api burst=20 nodelay;
    proxy_request_buffering on;
    proxy_connect_timeout 10s;
    proxy_read_timeout 120s;
  }

  location /.well-known/ {
    proxy_pass http://metis-skopos:8502;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  location /ai-market/ {
    proxy_pass http://metis-skopos:8502;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  location /aimarket/ {
    proxy_pass http://metis-skopos:8502;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 512k;
  }

  # Streamlit dashboard (subpath /app — see STREAMLIT_SERVER_BASEURLPATH=app)
  location = /app {
    return 301 /app/;
  }
  location /app/ {
    proxy_pass http://metis-skopos:8501/app/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 86400;
  }

  # Public marketing landing (static)
  location / {
    root /usr/share/nginx/html/skopos;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}


# --- Azimuth land (openazimuth.com) ---
# TLS: /var/lib/metis/letsencrypt (mounted at /etc/letsencrypt in metis-nginx)
# Renew: weekly cron mounts same path + webroot /var/www/certbot, then nginx -s reload
server {
  listen 80;
  server_name openazimuth.com www.openazimuth.com;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  location / { return 301 https://openazimuth.com$request_uri; }
}

server {
  listen 443 ssl;
  http2 on;
  server_name openazimuth.com www.openazimuth.com;
  ssl_certificate     /etc/letsencrypt/live/openazimuth.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/openazimuth.com/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;
  add_header Strict-Transport-Security "max-age=31536000" always;
  root /usr/share/nginx/openazimuth;
  index index.html;
  location / { try_files $uri $uri/ /index.html; }
}

# --- Azimuth demo HUD (demo.openazimuth.com) ---
server {
  listen 80;
  server_name demo.openazimuth.com;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl;
  http2 on;
  server_name demo.openazimuth.com;
  ssl_certificate     /etc/letsencrypt/live/openazimuth.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/openazimuth.com/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;
  add_header Strict-Transport-Security "max-age=31536000" always;

  resolver 127.0.0.11 ipv6=off valid=10s;

  location /v1/ {
    set $azimuth_api http://api:8080;
    proxy_pass $azimuth_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 300s;
  }
  location = /health {
    set $azimuth_api http://api:8080;
    proxy_pass $azimuth_api/health;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
  location / {
    set $azimuth_web http://web:80;
    proxy_pass $azimuth_web;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
